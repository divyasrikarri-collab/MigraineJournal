<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the real theme; this only needs to give the window a sane
      background and no action bar before the first frame.
    -->
    <style name="Theme.MigraineJournal" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
